<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Vídeos</title>
  <style>
    body {
      background-color: #f5f5f5;
      font-family: sans-serif;
    }

    .container {
      width: 900px;
      margin: 0 auto;
      margin-top: 50px;
      background-color: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.3);
      text-align: center;
    }

    h1 {
      margin-top: 0;
    }

    .card {
      margin-top: 20px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.3);
      overflow: hidden;
      text-align: left;
      display: inline-block;
      width: calc(33.33% - 20px);
      margin-right: 20px;
    }

    .card img {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .card-content {
      padding: 20px;
      color: white;
      background-color: #337ab7;
      display: flex;
      min-height: 200px;
      flex-direction: column;
      justify-content: space-between;
    }
    .card-content a{
        display: flex;
        align-items: center;
        justify-content: center;
        color: #337ab7;
        background-color: white;
        height: 30px;
        border-radius: 10px;
        text-decoration: none;
    }
    .container p{
        font-size: 30px;
        font-weight: bold;
        color: #337ab7;
    }
    
  </style>
</head>
<body>
    <div style="text-align: right;">
        <%= link_to "Voltar", "/", class: "btn btn-primary back_buttom" %>
    </div>
  <div class="container">
    <p>Watch Party</p>
    <% if @salas %>
        <% @salas.each do |sala| %>
        <div class="card">
            <div class="card-content">
                <div class='title'>Titulo: <%= sala.title ? sala.title : 'Conteudo vazio' %></div>
                <div class='nome'>Transmitido por: <%= sala.nome ? sala.nome : 'Conteudo vazio' %></div>
                <div class='url'>O que esta assistindo: <%= sala.url ? sala.url : 'Conteudo vazio' %></div>
                <div class='description'>Descricao: <%= sala.description ? sala.description : 'Conteudo vazio' %></div>
                <a href = "video/<%= sala.id %>">Visualizar sala</a>
            </div>
        </div>
        <% end %>
    <% end %>
    <br>
    <div class="text-left">
        <%= link_to 'Criar Sala', new_sala_path, class: 'btn btn-primary' %>
    </div>

  </div>
</body>
</html>
